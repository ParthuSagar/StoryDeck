import React, { useState } from 'react'
import {
  Box,
} from '@mui/material'

const Settings = () => {
  

  return (
    <Box
      sx={{
        width: '100%',
        minHeight: '100%',
      }}
    >
    </Box>
  )
}

export default Settings
